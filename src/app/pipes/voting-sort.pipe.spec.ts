import { VotingSortPipe } from './voting-sort.pipe';

describe('VotingSortPipe', () => {
  it('create an instance', () => {
    const pipe = new VotingSortPipe();
    expect(pipe).toBeTruthy();
  });
});
